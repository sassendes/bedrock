apiVersion: postgresql.cnpg.io/v1
kind: Pooler
metadata:
  name: cluster-1-pooler-rw
  namespace: cluster-1
spec:
  cluster:
    name: cluster-1
  instances: 2
  type: rw
  pgbouncer:
    poolMode: transaction
    parameters:
      max_client_conn: "1000"
      default_pool_size: "25"
